* Loader type (MYSQL, CSVALL).
LOADER_TYPE = CSVALL

* Investigated Data Layers (if any, NO_LAYER otherwise).
CONSIDERED_LAYERS = NO_LAYER


* Data Partitioning.

* File Used for Filtering
FILTERING_CSV_FILE = UNSW-NB15/UNSW-NB15_Analysis.csv

* Golden Runs.
GOLDEN_RUN_IDS = 0-5

* File Used for Training
TRAIN_CSV_FILE = UNSW-NB15/UNSW-NB15_Analysis.csv

* Training Runs.
TRAIN_RUN_IDS = 97-106

* File Used for Evaulating
VALIDATION_CSV_FILE = UNSW-NB15/UNSW-NB15_Analysis.csv

* Evaluation Runs.
VALIDATION_RUN_IDS = 107-115


* Parsing Dataset

* Features to Skip
SKIP_COLUMNS = 0, 2, 3, 4, 43, 44

* Column Containing the 'Label' Feature
LABEL_COLUMN = 43

* Size of Each Experiment
EXPERIMENT_ROWS = 500

* Tags Identifying an Attack in This Dataset
FAULTY_TAGS = Analysis, "Analysis"
